---
import Layout from '../../../layouts/Layout.astro';
import { DashboardShell } from '../../../components/DashboardShell';
import { UserAdmin } from '../../../components/Admin/UserAdmin';
import { AuthProvider } from '../../../components/AuthProvider';
---

<Layout title="Administración de Usuarios">
  <AuthProvider client:only="react">
    <DashboardShell title="Gestión de Personal" client:load>
      <div class="mb-6 border-b border-slate-800 pb-4">
        <p class="text-slate-400">
          Desde aquí puedes gestionar a los técnicos y encargados de QA. Solo los usuarios activos aquí aparecerán en los registros de Bases y Fabricación.
        </p>
      </div>
      <UserAdmin client:load />
    </DashboardShell>
  </AuthProvider>
</Layout>