---
import Layout from '../../layouts/Layout.astro';
import { DashboardShell } from '../../components/DashboardShell';
import { FabricacionForm } from '../../components/Fabricacion/FabricacionForm';
import { AuthProvider } from '../../components/AuthProvider';
---

<Layout title="Nueva Fabricación | Cimasur">
  <AuthProvider client:only="react">
    <DashboardShell title="Control de Producción" client:only="react">
      <div class="mb-6">
        <a href="/dashboard" class="text-slate-500 hover:text-white transition-colors text-sm">← Volver al Centro de Operaciones</a>
      </div>
      <FabricacionForm client:only="react" />
    </DashboardShell>
  </AuthProvider>
</Layout>